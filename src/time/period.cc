/**
 * @file period.cc
 *
 * Implementation of time period logic.
 *
 * Responsibilities:
 * - Implement logic to parse periods from standard string formats (e.g.,
 * parsing "3M", "1Y", "2W" from market data).
 * - Implement formatting logic to serialize periods back to strings.
 * - Provide normalization logic (e.g., ensuring "12 Months" is mathematically
 * treated equivalently to "1 Year").
 */
