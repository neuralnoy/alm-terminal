/**
 * @file period.h
 *
 * Defines explicit time intervals (Tenors) used in financial modeling.
 *
 * Responsibilities:
 * - Define a 'Period' (or Tenor) representing an exact duration of time (e.g.,
 * "3 Months", "5 Years", "1 Week").
 * - Provide utilities for date arithmetic using periods (e.g., adding "6
 * Months" to a start date).
 * - Serve as the standard vocabulary for describing the maturity, reset
 * frequency, or payment interval of an instrument.
 */
