/**
 * @file frequency.cc
 *
 * Implementation of frequency and tenor operations.
 *
 * Responsibilities:
 * - Implement parsing logic to convert string abbreviations (e.g., "3M", "1Y",
 * "SA") from market data feeds or UI inputs into native Tenor/Frequency
 * objects.
 * - Implement string formatting for outputting frequencies and periods.
 * - Provide logic for mathematical operations on frequencies (e.g., determining
 * the number of periods within a specific date range).
 */
